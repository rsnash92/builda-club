{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/((?!images|_next|favicon.ico).*)",
      "destination": "/app/$1"
    }
  ]
}
